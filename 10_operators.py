
x=10
y=7

x + y	 #  Addition
x - y	 #  Subtraction
x * y	 #  Multiplication
x / y	 #  Division
x % y	 #  Modulus
x ** y	 # 	Exponentiation
x // y   #  Floor division


# Python assignment Operator
"""
=	    x = 5	    x = 5	
+=	    x += 3	    x = x + 3	
-=	    x -= 3	    x = x - 3	
*=	    x *= 3	    x = x * 3	
/=	    x /= 3	    x = x / 3	
%=	    x %= 3	    x = x % 3	
//= 	x //= 3	    x = x // 3	
**= 	x **= 3	    x = x ** 3	
&=	    x &= 3	    x = x & 3	
|=	    x |= 3	    x = x | 3	
^=	    x ^= 3	    x = x ^ 3	
>>=	    x >>= 3	    x = x >> 3	
<<=	    x <<= 3	    x = x << 3	
:=	print(x := 3)	x = 3
                    print(x)
"""

# ==	Equal	x == y	
# !=	Not equal	x != y	
# >	Greater than	x > y	
# <	Less than	x < y	
# >=	Greater than or equal to	x >= y	
#<=	Less than or equal to	x <= y

'''
& 	AND	            Sets each bit to 1        	                        x & y	
                    if both bits are 1 

|	OR	            Sets each bit to 1   	                            x | y
                    if one of two bits is 1 

^	XOR      	    Sets each bit to 1  	                            x ^ y
                    if only one of two bits is 1	

~	NOT	            Inverts all the bits	                             ~x	

<<	Zero fill  	    Shift left by pushing zeros in from the right       x << 2	
    left shift      and let the leftmost bits fall off
    
>>	Signed right 	Shift right by pushing copies 	                    x >> 2
       shift        of the leftmost bit in from the left, 
                    and let the rightmost bits fall off
'''